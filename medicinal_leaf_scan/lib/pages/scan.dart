// lib/screens/scan.dart
import 'package:flutter/material.dart';

class ScanScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Scan Screen'),
      ),
      body: const Center(
        child: Text('Welcome to the Scan Screen!'),
      ),
    );
  }
}
